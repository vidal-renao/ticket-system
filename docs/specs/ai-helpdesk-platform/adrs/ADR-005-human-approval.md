# ADR-005: Human approval

**Status:** Proposed. Classification, retrieval, SLA calculation, summaries, priority suggestions and response drafts may run automatically. Customer sends, closure, critical priority changes, cross-team reassignment, external actions, ambiguous resend and low-confidence use require approval. Approval is organization/role scoped, expires, and binds the exact payload hash; every request and decision is append-only audited.

